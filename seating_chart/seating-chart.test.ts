import getMaxAdditionalDinersCount from "./seating-chart";

describe("", () => {
  it("", () => {
    const response = getMaxAdditionalDinersCount(10, 1, 2, [2, 6]);

    expect(response).toBe(3);
  });
});
